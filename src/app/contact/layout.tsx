export const metadata = {
  title: "Contact",
  description: "Get in touch with Boomerang Pilates. Questions about classes, pricing, or private sessions in Durham, NC.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
